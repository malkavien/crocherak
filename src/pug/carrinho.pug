doctype html
html(lang='pt-br')
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    title Carrinho | Rak Crôche
    link(rel='stylesheet', href='css/styles.css')
    link(href='https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css', rel='stylesheet')

  body
    .container.mt-5
      .d-flex.justify-content-between.align-items-center.mb-4
        h1.mb-0 Carrinho de Compras
        a.btn.btn-secondary(href='/index.html') ⬅ Voltar para a Loja

      .alert.alert-info.mt-4
        strong Atenção:
        |  ao finalizar a compra, uma mensagem será enviada para o WhatsApp da lojista. 
        | A partir daí, o atendimento continuará por lá, incluindo confirmação da disponibilidade do item, formas de pagamento e detalhes sobre envio ou entrega.

      table.table.table-bordered.mt-4
        thead
          tr
            th Produto
            th Quantidade
            th Preço
            th Total
            th Ação
        tbody#cart-items
          //- Os itens vão ser inseridos via JavaScript

      .row.mt-4
      .col-md-6
        label(for='cep') CEP para cálculo do frete:
        input#cep.form-control(type='text', placeholder='Digite o CEP', maxlength='9')

      .col-md-6
        label Frete:
        p#frete-value.fw-bold R$ 0,00

      .text-end.fw-bold.fs-5.mt-3
        | Total (com frete): R$
        span#cart-total 0,00

      .text-end.mt-4
        button.btn.btn-danger#clear-cart Limpar Carrinho
        button.btn.btn-success.ms-2#checkout Finalizar Compra

    script(src='https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js')
    script(src='js/cart.js')
    script(src='js/cart-view.js')
